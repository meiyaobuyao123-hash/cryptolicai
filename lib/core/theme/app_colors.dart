import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ── Backgrounds ──
  static const scaffoldBg = Color(0xFFFFFFFF);
  static const cardBg = Color(0xFFFFFFFF);

  // ── Text ──
  static const label = Color(0xFF2C2C2C);
  static const secondaryLabel = Color(0xFF999999);
  static const tertiaryLabel = Color(0xFFCCCCCC);

  // ── Lines ──
  static const separator = Color(0xFFF0F0F0);
  static const border = Color(0xFFE8E8E8);

  // ── Fills ──
  static const fill = Color(0xFFF5F6F8);

  // ── Accents ──
  static const accent = Color(0xFF2563EB);
  static const green = Color(0xFF22C55E);
  static const red = Color(0xFFEF4444);
  static const orange = Color(0xFFF97316);
  static const purple = Color(0xFF8B5CF6);
  static const teal = Color(0xFF14B8A6);
  static const indigo = Color(0xFF6366F1);
  static const pink = Color(0xFFDB2777);
  static const yellow = Color(0xFFCA8A04);
  static const gray = Color(0xFF9CA3AF);

  // ── Chart ──
  static const chartColors = [
    Color(0xFF2563EB), Color(0xFF22C55E), Color(0xFF8B5CF6),
    Color(0xFFF97316), Color(0xFFEF4444), Color(0xFF14B8A6),
    Color(0xFF6366F1), Color(0xFFDB2777), Color(0xFFCA8A04), Color(0xFF0891B2),
  ];

  // ── Icons ──
  static const iconBgBlue = Color(0xFFEFF6FF);
  static const iconBgGreen = Color(0xFFF0FDF4);
  static const iconBgPurple = Color(0xFFF5F3FF);
  static const iconBgOrange = Color(0xFFFFF7ED);
  static const iconBgRed = Color(0xFFFEF2F2);
  static const iconBgTeal = Color(0xFFF0FDFA);

  // ── Ranking ──
  static const gold = Color(0xFFD97706);
  static const silver = Color(0xFF9CA3AF);
  static const bronze = Color(0xFFB45309);
}
